#берем последнюю версию ubuntu
FROM ubuntu:latest  
# Обновляем список пакетов и устанавливаем пакеты с авто соглашением на все пункты
RUN apt-get update && apt-get install -y \ 
# установка пакета x11
x11-apps \ 
#установка пакета xauth
xauth \
#установка пакета open jdk 11
openjdk-11-jre\
#Для уменьшения размера образа Docker удаляем apt кеш после установки пакетов
&& rm -rf /var/lib/apt/lists/*
#устанаваливаем new-apps как рабочую директорию
WORKDIR /new-apps
#копируем файлы из текущей директории и переносим в директорию, созданную внутри контейнера
COPY calc.jar /new-app/
#Переменная среды DISPLAY сообщает программе с графическим интерфейсом, как взаимодействовать с графическим интерфейсом,
#:0 — первый запущенный локальный дисплей.
ENV DISPLAY=:0
#запуск java программы
CMD ["java" , "-jar" , "/new-app/calc.jar"]